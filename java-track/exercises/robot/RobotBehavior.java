package robot;

public interface RobotBehavior {//interfaces just have behaviors, no constructors

		public void doNextMove(Robot r, Robot m);//never say how the method will work in interface because it depends on the actual object
		public void speak();
		
				
			
		}
		

